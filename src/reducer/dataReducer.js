import { ACTIONS } from "../utils/ACTIONS";

export const initialDataState = {};

export const dataReducer = (draft, action) => {
  switch (action.type) {
    case ACTIONS.TEST:
      break;

    default:
      break;
  }
};
